[build-system]
requires = ["uv_build>=0.11.28,<0.12"]
build-backend = "uv_build"

[project]
name = "r0s-pr-read-model"
version = "0.1.0"
description = "GitHub PR collection and classification model"
requires-python = ">=3.12"
dependencies = []
